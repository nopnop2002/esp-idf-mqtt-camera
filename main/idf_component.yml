## IDF Component Manager Manifest File
dependencies:
  espressif/esp32-camera:
    version: '>=2.0.3'
  espressif/mdns:
    version: "^1.0.3"
    rules:
      - if: "idf_version >=5.0"
  espressif/mqtt:
    version: "^1.0.0"
    rules:
      - if: "idf_version >=6.0"
